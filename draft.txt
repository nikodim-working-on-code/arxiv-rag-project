🚀 RAG Pet Project: AI-Powered Research Assistant для arXiv Papers

📋 Идея проекта


Проблема:

	На arXiv.org публикуется 500+ научных статей в день (ML/AI)

	Исследователям сложно следить за новыми статьями и находить релевантные работы

	Читать полные PDF долго → нужны быстрые саммари и Q&A по статьям


Решение:
	RAG-система, которая:

		Автоматически скачивает новые статьи с arXiv (ML/AI категории)

		Индексирует их в vector database

		Позволяет задавать вопросы на естественном языке

		Генерирует summaries для новых статей

		Сравнивает статьи (find similar papers)

		Отслеживает тренды (какие топики популярны)



🛠️ Технологический стек

Core (обязательные):
	1) Backend Framework:

		FastAPI (Python) — быстрый, async, автодокументация ✓

	2) LLM:

		OpenAI GPT-4o-mini (дешевый, быстрый для RAG) или

		Llama 3.1 8B (бесплатный через Ollama/Together AI)

	3) Embeddings:

		OpenAI text-embedding-3-small (1536 dims, $0.02/1M tokens) или

		sentence-transformers/all-MiniLM-L6-v2 (бесплатный, 384 dims, быстрый)

	4) Vector Database:

		Chroma (простой, in-memory/persistent) или

		Weaviate (более мощный, hybrid search)

	5) PDF Processing:

		PyMuPDF (fitz) — извлечение текста из PDF

		LangChain Document Loaders — chunk splitting

	6) Data Source:

		arXiv API — бесплатный, RSS feed для новых статей


Advanced (для усиления проекта):

	7) Reranking (улучшение retrieval):

		Cohere Rerank API (топ качество, $1/1K requests) или

		CrossEncoder (ms-marco-MiniLM, локальный)

	8) Caching:

		Redis — кешировать embeddings, частые queries

	9) Monitoring:

		LangSmith (LangChain) — трассировка LLM calls, debugging

	10) Frontend:

		Streamlit (быстрый прототип) или

		Next.js + React (production-ready UI)

	11) Async Processing:

		Celery + RabbitMQ — background tasks (скачивание PDF, индексация)

	12) Database (metadata):

		PostgreSQL + pgvector — хранить metadata (авторы, даты, categories) + векторы




┌─────────────────────────────────────────────────────────┐
│                    USER (Frontend)                      │
│              Streamlit / React Web App                  │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│                  FastAPI Backend                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │
│  │   Query      │  │   Ingest     │  │   Trends     │   │
│  │   Endpoint   │  │   Endpoint   │  │   Endpoint   │   │
│  └──────────────┘  └──────────────┘  └──────────────┘   │
└────────┬──────────────────┬──────────────────┬──────────┘
         │                  │                  │
         ▼                  ▼                  ▼
┌────────────────┐  ┌────────────────┐  ┌────────────────┐
│ Vector DB      │  │ arXiv API      │  │ PostgreSQL     │
│ (Chroma/       │  │ + PDF Parser   │  │ (Metadata)     │
│  Weaviate)     │  │                │  │                │
└────────────────┘  └────────────────┘  └────────────────┘
         │
         ▼
┌────────────────────────────────────────────────────────┐
│                  LLM (GPT-4o-mini)                     │
│              + Reranker (Cohere/CrossEncoder)          │
└────────────────────────────────────────────────────────┘



2) Метрики:

	Query latency (p50, p95, p99)

	Cache hit rate (Redis)

	Daily indexed papers

	User query distribution (top queries)



🔥 Extensions (если захочешь продолжить)

	1) Citation Graph:

		Парсить references из PDF → строить граф связей между статьями

		"What papers cite this paper?"

	2) Author Profiles:

		Aggregate papers по авторам

		"Show me all papers by Yann LeCun"

	3) Multi-language Support:

		Embeddings (multilingual models) → Q&A на русском/китайском

	4) Alerts:

		Email уведомления: "New papers on [your topic] published!"

	5) Compare Papers:

		"Compare methodology of paper A vs paper B"

	6) Visual Search:

		Upload image (diagram/plot) → find papers with similar figures



📊 Лимиты arXiv API

	1) Rate Limits (скорость запросов)
	
		Официальные ограничения:

			1 запрос каждые 3 секунды (20 запросов в минуту)​

			1 connection одновременно (нельзя параллельные запросы)


		Для скачивания PDF:

			4 запроса в секунду с 1-секундной паузой после каждого burst

			Используй export.arxiv.org вместо arxiv.org (меньше нагрузка на основной сайт)

	
	2) Максимум результатов за запрос

		Metadata (через API):

			30,000 результатов максимум за один query
​
			Нужно делать batches по 2000 (параметры start и max_results)

			Запрос на 30K результатов = ~2 минуты + ~15 MB данных
​

		На практике (библиотека arxiv.py):

			Некоторые источники говорят о лимите 300,000 результатов
​
			Но реально безопасно работать с 30K-50K за раз



